version: '3.4'

services:
  binancechartsvg:
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      - PORT=8000
    ports:
      - 4000:8000
  redis:
    image: redis:6.2.6
    ports:
      - 6379:6379